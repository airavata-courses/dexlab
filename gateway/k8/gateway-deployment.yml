apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeservice
spec:
  selector:
    matchLabels:
      run: nodeservice
  replicas: 3
  template:
    metadata:
      labels:
        run: nodeservice
    spec:
      containers:
        - name: nodeservice
          image: kausau/gateway:c8391d0d9d33f975720aa27036ae549c6d6e640c
          env:
            - name: redisIp
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: redisIp
            - name: rabbitIp
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: rabbitIp
            - name: tokenPassword
              valueFrom:
                secretKeyRef:
                  name: gateway-secret
                  key: tokenPassword
          ports:
            - containerPort: 3001
